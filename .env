# -------------------------------
# MySQL Database Configuration
# -------------------------------
# Format: mysql://user:password@host:port/dbname
DATABASE_URL=mysql://root:root@localhost:3306/leave_mcp

# Alternatively, you can use separate variables:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=leave_mcp
DB_PORT=3306

# -------------------------------
# MCP Server Configuration
# -------------------------------
# Transport: streamable-http for cloud, stdio for local
MCP_TRANSPORT=streamable-http

# Host and Port for MCP server
MCP_HOST=0.0.0.0
PORT=8080

# Optional: API key for Smithery registry (if using MCP registry)
SMITHERY_API_KEY=33285d9f-64ad-400f-8650-a90ed8e26a23
# -------------------------------
# Optional: Debug/Logging
# -------------------------------
DEBUG=True
