name: my-mcp-server
runtime: python
entrypoint: server.py
dependencies:
  - mysql-connector-python
  - fastmcp
  - python-levenshtein
build:
  commands:
    - pip install -r requirements.txt
startCommand:
  command: python
  args:
    - server.py
