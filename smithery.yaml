name: my-mcp-server
type: python
entrypoint: app.py       # Your main script
dependencies:
  - python>=3.11
  - mysql-connector-python
  - fastmcp
build:
  commands:
    - pip install -r requirements.txt
